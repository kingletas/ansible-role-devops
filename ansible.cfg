[defaults]
# The parent finds a checkout named devops; .ansible/roles is where
# scripts/link-role links any other checkout under that name.
roles_path = ../:roles:.ansible/roles
nocows = true
result_format = yaml
