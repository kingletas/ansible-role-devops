# The linters `make check` runs and the Molecule stack `make converge` runs.
# CI installs exactly these versions.
ansible-core==2.21.4
ansible-lint==26.8.0
yamllint==1.38.0
molecule==26.8.0
molecule-plugins[docker]==26.7.15
docker==7.2.0
